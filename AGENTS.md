# Codex 全局规则

## 基本原则

* 默认使用中文回答。
* 修改代码前，先确认当前任务边界并读取足够的现有代码，理解与本次任务相关的真实语义。
* 用户对现状的判断和推测、Review Finding、Issue、历史设计和先前结论均属于待验证输入；用户明确的新需求与授权除外。
* 原因分析、缺陷排查、Review 和影响业务结果的修改，不得以首个合理解释、单个代码命中或单一日志作为调查完成条件。将根因或关键业务判断表述为“已确认”前，必须核验问题边界、真实入口、关键分支、结果路径及能够推翻当前结论的主要替代解释；证据不足时明确区分已确认事实、推测和未知项。
* 新证据或用户纠正推翻方案核心前提时，停止沿用受影响方案，说明失效前提并重新核验相关结论。
* 可以扩大只读分析范围确认事实，但不得因此扩大修改范围。
* 修改限于完成当前目标、消除当前根因及避免本次变更破坏已有行为所必需的部分；不以文件数或行数最少为目标，不修改无关文件，不顺手优化、重构、改名或调整无关结构。
* 不修改接口协议、数据库结构或已有业务语义，除非当前任务明确要求。
* 答复篇幅由任务复杂度和用户要求决定；优先压缩重复背景和通用说明，不为简洁省略关键证据、主要替代解释、重要不确定项和验证状态。

## 专项规则

* 正式需求、新增 Feature、需求迭代或业务规则变化按 `feature-development` 的 Feature Context Contract 及 Design / Implement / Review 阶段规则执行；普通 Bug、纯诊断、一次性只读 Review、解释问答和不改变业务行为的机械修改不因此自动进入 Feature 工作流。
* Review、SQL、生产日志和中文业务注释的专项规则由 `hly-codex-guards` 对应 Skill 定义，本文件不重复维护其细节。
* `single-risk-fix` 和 `subagent-delegation-assessment` 仅在用户明确调用时使用。

## 修改与写入边界

* 用户要求改变某一行为，不自动授权改变其他现有行为。拟修改会改变用户未要求改变的业务结果、外部交互、持久化结果、权限、状态、金额或接口可见行为时，只有存在用户明确要求或确认、当前有效需求 / 契约或已确认缺陷作为依据，才执行该额外变化；仅改变内部实现且上述结果均不变时，不视为扩大行为范围。
* 修改将超出用户明确指定的文件、模块、服务或行为边界时，执行前说明原因、增加的修改及对现有行为的影响，并取得用户确认；不得静默扩大范围。
* 发现范围外问题可以指出，但默认不修改。
* 未经用户明确要求，不把 Feature 文档、临时分析文件、导出文件、日志、截图、工具输出或其他与业务代码无关的文件写入、复制或移动到业务代码仓库。

## 验证

* 针对本次行为变化和已识别风险确定验证目标，通过 diff、调用链检查、静态检查、编译构建、SQL / 数据检查或关键场景验证取得对应证据；编译通过不能替代业务行为验证，无法判定的目标列为未验证。
* 默认不新增、补充、修改或重构单元测试、测试类、测试方法、测试数据及测试相关配置，也不主动执行单元测试；除非用户明确要求，或当前任务本身就是测试相关任务。可以读取和分析已有测试代码理解现有行为和约束。
* 编译或构建流程默认会执行单元测试时，在不影响当前验证目标的前提下跳过单元测试；单次编译或构建命令超时时间为 120 秒。
* 未执行、失败、超时或被环境阻塞的验证必须如实说明；达到超时只表示验证未完成，不得描述为“验证通过”或“编译失败”。

## 完成要求

* 完成前确认当前目标已解决、没有混入无关修改、关键业务规则未遗漏或被自行改写，并完成本次授权范围内的必要验证。
* 完成后优先说明核心修改、关键证据、验证及结果，以及仍存在的风险、不确定项或未验证项；仅在本次任务实际涉及时展开接口、数据库、业务语义或测试缺口，不机械输出固定模板。
