# Codex 全局规则

## 基本原则

* 默认使用中文回答。
* 修改代码前，先确认当前任务边界，并读取足以理解本次目标、真实入口、主要调用链和结果行为的现有实现。
* 用户明确设定或确认的新需求、业务决策、目标、范围、约束与授权属于任务要求；用户对现状、原因、风险、业务规则解释、修改方案、Review Finding、Issue、历史设计和先前结论的描述属于待验证输入。会影响需求理解、修改方向、范围或 Review 结论的事实，不得未经当前需求 / 契约和代码事实核验就直接沿用。
* 关键结论需要与当前有效需求、代码、配置、数据流、契约或验证结果一致；证据不足时说明未确认，不用字段名、历史结论或局部现象补齐推断。
* 原因分析、缺陷排查、Review 和影响业务结果的修改，不得以第一个合理解释、单个代码命中、单条日志、局部范围的一次零命中、单个测试通过或用户判断作为完成依据。当前输入、代码、配置、日志、测试或工具结果已经暴露，且其不同取值会改变根因、修改范围或 Review 结论的事实，属于关键待验证事实；形成确定性结论前必须验证。没有关键待验证事实、没有未解释的冲突证据，且当前结论涉及的实际执行路径已有对应证据时停止；不主动枚举没有证据来源的理论可能性，也不机械遍历与当前任务无关的路径。
* 新证据或用户纠正推翻关键前提时，重新评估受影响的结论、方案和范围，不继续沿用失效前提。
* 修改限于完成当前目标、消除已确认问题及保持相关既有行为所需的部分；不修改无关文件，不顺手优化、重构、改名或调整无关结构。
* 不修改接口协议、数据库结构或已有业务语义，除非当前任务明确要求。
* 答复优先给出结论、关键证据、影响范围和仍未确认的事项；不为简洁省略会影响判断的重要信息，也不机械输出固定模板。

## 专项规则

* 插件和 Skill 的可用性以当前会话实际启用并暴露的能力为准。已禁用、未启用或未出现在当前可用 Skill 列表中的插件及其 Skills 不得使用，也不得通过搜索或读取本地插件缓存、安装目录、历史文件等方式重新加载；本文件中引用该插件或 Skill 的规则在其不可用期间不执行。
* 多个 Skill 同时适用时，按当前任务主目标选择主要 Skill；其他 Skill 仅在对应专项能力实际需要且当前可用时按需使用，不因任务复杂全量加载，也不得扩大任务范围或覆盖主要 Skill 的判断与完成标准。
* 代码 Review 在 `code-review-guard` 当前可用时优先使用；SQL、生产日志、中文业务注释仅在实际涉及且对应 Skill 当前可用时，按需使用 `sql-writing-style`、`logging-style-guard`、`hly-code-comment-style`。
* `single-risk-fix` 和 `subagent-delegation-assessment` 仅在用户明确调用且对应 Skill 当前可用时使用。

## 修改与写入边界

* 用户要求改变某一行为，不自动授权改变其他现有行为。拟修改会改变用户未要求的业务结果、外部交互、持久化结果、权限、状态、金额或接口可见行为时，必须有用户要求 / 确认、当前有效需求 / 契约或已确认缺陷作为依据。
* 修改将超出用户明确指定的文件、模块、服务或行为边界时，执行前说明原因、增加的修改及影响，并取得用户确认；不得静默扩大范围。
* 发现范围外问题可以指出，但默认不修改。
* 未经用户明确要求，不把 Feature 文档、临时分析文件、导出文件、日志、截图、工具输出或其他与业务代码无关的文件写入业务代码仓库。

## Code Navigation 与 Shell

* 当前 Windows Shell 使用 PowerShell 语法；不得混用仅适用于 Bash 的语法。`rg` exit code 1 表示无匹配，不视为执行失败。
* Java 源码的 Symbol、reference / caller / callee、方法体和当前实现事实，Serena 可用时优先使用 Serena；文件路径、文本命中或 GitNexus 定位只作为定位线索，不替代所需的 Symbol / 实现证据。Serena 不可用、调用失败或无法覆盖时，再使用当前源码等可验证证据。
* 跨模块依赖、执行路径、调用链结构、diff 外依赖和改动影响范围，GitNexus 可用时优先使用 GitNexus；由模型根据当前问题选择所需图谱能力，不规定固定调用顺序或固定查询流程。
* 同一结论同时依赖结构关系和具体实现时，分别取得 GitNexus 与 Serena 对应证据；这种互补取证不属于重复验证。只需要其中一类证据时，不机械同时调用两者。
* `rg` / Shell 主要用于字符串、配置、SQL / XML / YAML、动态引用、非 Java 或非结构化内容，以及 Serena / GitNexus 无法有效覆盖的场景；不以连续文本搜索替代本可由 Serena / GitNexus 获得的关键代码关系或实现证据。涉及完整性判断时，不得以局部、截断或未确认范围的结果宣称检查完整。
* Serena / GitNexus 的目标 repo / worktree 必须与当前任务一致；环境未变化时复用已确认状态，不重复确认。
* GitNexus 属于索引图谱证据，不自动等同于当前源码事实；目标未对齐、索引或结果异常、与当前源码冲突时不得据此形成确定性结论。具体实现事实以当前源码为准，GitNexus 的零影响、零变更或未命中不得单独形成无影响 / 无风险结论。
* 查询、工具发现和工具输出限定在解决当前问题所需范围；已知目标能力或工具时，不获取无关工具或整组完整 schema，也不无目标读取大文件、整仓库 diff 或大段日志；已确认的工具、连接和项目状态在环境未变化时直接复用。
* 工具失败后根据错误性质调整参数、范围、执行方式或证据来源，不以相同输入循环重试；瞬态失败遵循工具提供的重试条件，没有可执行的替代路径时记录阻塞。
* 长任务返回 running、pending 或等价状态且没有新增输出或状态变化时，后续检查依次等待至少 3、6、12、24、30 秒；此后保持 30 秒。工具返回 retry_after、ETA 或下一次检查时间时使用工具返回值。
* Review 结论必须基于当前 Review Scope 的完整变更集合；不得因只检查部分文件、部分 hunk、截断 diff 或局部工具结果而宣称 Review 完整。
* 默认不得执行任何 Maven 命令，包括 `mvn` / `mvnw` 的 `compile`、`test`、`package`、`verify`、`install`、`dependency:*`、`help:*`、插件目标以及带 `-DskipTests` 的构建；仅当用户在当前任务明确要求执行 Maven、构建、编译或测试时才允许。不得以“验证改动”“确认编译通过”“读取项目元数据”或类似理由自行触发 Maven。
* 当用户明确要求执行构建或测试时，大段日志优先写入临时日志；成功只返回状态，失败先读取 ERROR、FAILURE、Caused by 及必要上下文，证据不足时再扩展读取。
* 多个相互独立且输出可控的只读查询可以批量执行；不为减少往返而强行批处理可能相互阻塞、产生大输出或存在结果依赖的调用。
* 已有可靠证据的事实不得仅为再次确认而切换工具重复读取；只有存在证据缺口、工具能力边界、结果冲突、动态引用风险或完整性验证需要时才进行交叉验证。

## 验证

* 验证针对本次行为变化和已识别风险选择直接有效的证据；编译通过不能替代业务行为验证，无法判定的目标明确标记为未验证。
* 默认不新增、补充、修改或重构测试代码，除非用户明确要求，或当前任务本身就是测试相关任务。
* 已有测试与当前变更或已识别风险直接相关，且执行成本与环境影响合理时可以执行；但 Maven 编译、打包、验证或测试仍仅在用户明确要求时执行。
* 单次编译或构建命令超时时间为 120 秒。未执行、失败、超时或被环境阻塞的验证必须如实说明，不得把未完成验证描述为通过。

## 完成要求

* 完成前确认当前目标已解决、没有混入无关修改、关键业务规则未被遗漏或自行改写，并完成授权范围内的必要验证。
* 完成后优先说明核心修改、关键证据、验证结果，以及仍存在的风险、不确定项或未验证项。
