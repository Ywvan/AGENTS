# Codex 全局规则

## 基本原则

* 默认使用中文回答。
* 修改代码前，先确认当前任务边界，并读取足以理解本次目标、真实入口、主要调用链和结果行为的现有实现。
* 用户明确设定或确认的新需求、业务决策、目标、范围、约束与授权属于任务要求；用户对现状、原因、风险、业务规则解释、修改方案、Review Finding、Issue、历史设计和先前结论的描述属于待验证输入。会影响需求理解、修改方向、范围或 Review 结论的事实，不得未经当前需求 / 契约和代码事实核验就直接沿用。
* 关键结论需要与当前有效需求、代码、配置、数据流、契约或验证结果一致；证据不足时说明未确认，不用字段名、历史结论或局部现象补齐推断。
* 调查以支持当前任务判断为目标：需要时扩大只读分析范围；已有证据足以支撑当前结论且没有已知冲突时停止，不为完整性机械遍历无关路径，也不因此扩大修改范围。
* 新证据或用户纠正推翻关键前提时，重新评估受影响的结论、方案和范围，不继续沿用失效前提。
* 修改限于完成当前目标、消除已确认问题及保持相关既有行为所需的部分；不修改无关文件，不顺手优化、重构、改名或调整无关结构。
* 不修改接口协议、数据库结构或已有业务语义，除非当前任务明确要求。
* 答复优先给出结论、关键证据、影响范围和仍未确认的事项；不为简洁省略会影响判断的重要信息，也不机械输出固定模板。

## 专项规则

* 正式需求、新增 Feature、需求迭代或业务规则变化按 `feature-development` 的 Feature Context Contract 及 Design / Implement / Review 阶段规则执行；普通 Bug、纯诊断、一次性只读 Review、解释问答和不改变业务行为的机械修改不自动进入 Feature 工作流。
* Skill 使用“Primary + Conditional Specialist”：Primary Skill 负责当前任务生命周期；只有当前变更实际涉及对应领域时才叠加 Specialist Skill。不要因为任务复杂全量加载所有 Skill，也不要让 Specialist 接管整体任务。
* 不属于正式 Feature 的一次性代码 Review 以 `code-review-guard` 为 Primary；正式 Feature 的最终 Review 以 `feature-review` 为 Primary，并组合 `code-review-guard`。SQL、生产日志、中文业务注释仅在实际涉及时分别组合 `sql-writing-style`、`logging-style-guard`、`hly-code-comment-style`。
* `single-risk-fix` 和 `subagent-delegation-assessment` 仅在用户明确调用时使用。

## 修改与写入边界

* 用户要求改变某一行为，不自动授权改变其他现有行为。拟修改会改变用户未要求的业务结果、外部交互、持久化结果、权限、状态、金额或接口可见行为时，必须有用户要求 / 确认、当前有效需求 / 契约或已确认缺陷作为依据。
* 修改将超出用户明确指定的文件、模块、服务或行为边界时，执行前说明原因、增加的修改及影响，并取得用户确认；不得静默扩大范围。
* 发现范围外问题可以指出，但默认不修改。
* 未经用户明确要求，不把 Feature 文档、临时分析文件、导出文件、日志、截图、工具输出或其他与业务代码无关的文件写入业务代码仓库。

## 验证

* 验证针对本次行为变化和已识别风险选择直接有效的证据；编译通过不能替代业务行为验证，无法判定的目标明确标记为未验证。
* 默认不新增、补充、修改或重构测试代码，除非用户明确要求，或当前任务本身就是测试相关任务。
* 已有测试与当前变更或已识别风险直接相关，且执行成本与环境影响合理时可以执行；不因此自动扩展到全量测试。
* 单次编译或构建命令超时时间为 120 秒。未执行、失败、超时或被环境阻塞的验证必须如实说明，不得把未完成验证描述为通过。

## 完成要求

* 完成前确认当前目标已解决、没有混入无关修改、关键业务规则未被遗漏或自行改写，并完成授权范围内的必要验证。
* 完成后优先说明核心修改、关键证据、验证结果，以及仍存在的风险、不确定项或未验证项。
